# Digital Spotter - Python Dependencies
# Edge-based multimodal AI posture correction system
# Target: Raspberry Pi 4B 8GB RAM | Raspberry Pi OS Bookworm 64-bit (aarch64)
# Author: bimalawijekoon
# Version: 1.0.0

# Web framework
flask==3.0.3
flask-socketio==5.3.6
flask-cors==4.0.1
eventlet==0.36.1

# Computer vision & pose estimation
mediapipe
opencv-python-headless

# ML inference — tflite-runtime runs the real LSTM .tflite model on Pi
# Install via: pip install --break-system-packages tflite-runtime
# (Wheels for aarch64/Python3.11 served from piwheels.org)
tflite-runtime

# Signal processing — Savitzky-Golay filter for rep phase segmentation
scipy==1.13.1

# Serialisation — scaler.pkl loading
numpy==1.26.4
joblib==1.4.2

# IoT / sensor
paho-mqtt==2.1.0

# Testing
pytest==8.2.2
requests==2.32.3
